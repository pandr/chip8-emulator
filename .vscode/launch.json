{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Emulator",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/emulator",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
